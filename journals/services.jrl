p({
  "class": "foam.core.boot.CSpec",
  "name": "recipeDAO",
  "description": "",
  "serve": true,
  "authenticate": true,
  "keywords": [ "recipe" ],
  "serviceScript": """
    return new foam.dao.EasyDAO.Builder(x)
      .setOf(com.foamdev.cook.Recipe.getOwnClassInfo())
      .setPm(true)
      .setSeqNo(true)
      .setAuthorize(false)
      .setJournalType(foam.dao.JournalType.SINGLE_JOURNAL)
      .build();
  """,
  "client": `{"of":"com.foamdev.cook.Recipe"}`
})

p({
  "class": "foam.core.boot.CSpec",
  "name": "ingredientDAO",
  "description": "",
  "serve": true,
  "authenticate": true,
  "keywords": [ "ingredient" ],
  "serviceScript": """
    return new foam.dao.EasyDAO.Builder(x)
      .setOf(com.foamdev.cook.Ingredient.getOwnClassInfo())
      .setPm(true)
      .setSeqNo(true)
      .setAuthorize(false)
      .setJournalType(foam.dao.JournalType.SINGLE_JOURNAL)
      .build();
  """,
  "client": `{"of":"com.foamdev.cook.Ingredient"}`
})

p({
  "class": "foam.core.boot.CSpec",
  "name": "ingredientAmountDAO",
  "description": "",
  "serve": true,
  "authenticate": true,
  "keywords": [ "ingredientAmount" ],
  "serviceScript": """
    return new foam.dao.EasyDAO.Builder(x)
      .setOf(com.foamdev.cook.IngredientAmount.getOwnClassInfo())
      .setPm(true)
      .setSeqNo(true)
      .setAuthorize(false)
      .setJournalType(foam.dao.JournalType.SINGLE_JOURNAL)
      .build();
  """,
  "client": `{"of":"com.foamdev.cook.IngredientAmount"}`
})

p({
  "class": "foam.core.boot.CSpec",
  "name": "recipeStepDAO",
  "description": "",
  "serve": true,
  "authenticate": true,
  "keywords": [ "recipeStep" ],
  "serviceScript": """
    return new foam.dao.EasyDAO.Builder(x)
      .setOf(com.foamdev.cook.RecipeStep.getOwnClassInfo())
      .setPm(true)
      .setSeqNo(true)
      .setAuthorize(false)
      .setJournalType(foam.dao.JournalType.SINGLE_JOURNAL)
      .build();
  """,
  "client": `{"of":"com.foamdev.cook.RecipeStep"}`
})

p({
  "class": "foam.core.boot.CSpec",
  "name": "conversionService",
  "serve": true,
  "authenticate": true,
  "boxClass": "com.foamdev.cook.ConversionServiceSkeleton",
  "serviceScript": """
    return new com.foamdev.cook.ServerConversionService(x);
  """,
  "client": """
    {
      "class": "com.foamdev.cook.ClientConversionService",
      "delegate": {
        "class": "foam.box.SessionClientBox",
        "delegate": {
          "class": "foam.box.HTTPBox",
          "url": "service/conversionService"
        }
      }
    }
  """
})

p({
  "name": "recipeStepIngredientAmountJunctionDAO",
  "lazy": true,
  "serve": true,
  "description": "DAO responsible for storing the RecipeStep <-> IngredientAmount junction.",
  "serviceScript": """
    return new foam.dao.EasyDAO.Builder(x)
      .setJournalType(foam.dao.JournalType.SINGLE_JOURNAL)
      .setJournalName("recipeStepIngredientAmountJunction")
      .setOf(com.foamdev.cook.RecipeStepIngredientAmountJunction.getOwnClassInfo())
      .build();
  """,
  "client": `{"of":"com.foamdev.cook.RecipeStepIngredientAmountJunction"}`
})
